package com.school.service;

import org.springframework.stereotype.Service;

@Service
public class ExamService {

//    public Exam addExam(Exam exam) {
//        Long examTypeId = exam.getExamType().getExamTypeId(); // ensure it's sent in request
//        ExamType examType = examTypeRepository.findById(examTypeId)
//                .orElseThrow(() -> new ResourceNotFoundException("ExamType not found"));
//
//        exam.setExamType(examType); // set the relationship
//        return examRepository.save(exam); // save only Exam, not ExamType
//    }

}
